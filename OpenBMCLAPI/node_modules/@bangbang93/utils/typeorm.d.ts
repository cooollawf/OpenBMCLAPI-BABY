import { Connection, QueryRunner } from 'typeorm';
import { ReplicationMode } from 'typeorm/driver/types/ReplicationMode';
/**
 * withTransaction for typeorm
 * @param fn
 * @param connection
 * @param qr
 * @param mode
 */
export declare function withTransaction<T>(fn: (qr: QueryRunner) => Promise<T>, connection: Connection, qr?: QueryRunner, mode?: ReplicationMode): Promise<T>;
